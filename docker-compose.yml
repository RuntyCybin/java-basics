version: '3.8'

services:
  javabasics:
    image: maven:3.9.6-eclipse-temurin-17
    container_name: javabasics-app
    working_dir: /app
    volumes:
      - ./:/app
    command: mvn clean compile exec:java -Dexec.mainClass="com.example.Main"
    stdin_open: true
    tty: true